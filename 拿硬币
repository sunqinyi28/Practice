    public int minCount(int[] coins) {
        int times=0;
        for(int i=0;i<coins.length;i++){
            if(coins[i]%2==0){times+=coins[i]/2;}
            else{times+=(coins[i]+1)/2;}
        }
        return times;
    }
