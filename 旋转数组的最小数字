    public int minArray(int[] numbers) {
     /*   Arrays.sort(numbers);
        return numbers[0];*/
        int i=1;
        while(i<numbers.length){
            if(numbers[i]<numbers[i-1]) return numbers[i];
            i++;
        }
        return numbers[0];
    }
